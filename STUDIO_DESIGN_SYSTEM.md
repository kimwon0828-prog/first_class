# 첫수업 Studio 디자인 시스템 v1.1

파트너 센터(`/studio`) 전용 기준 문서. 학원 운영자가 쓰는 **업무 도구**의 규칙이다.
학부모 페이지(`/classes`, `/my`)에는 적용하지 않는다 — 그쪽은 480px 모바일 컬럼 체계로 별도 관리.

AI 코딩 에이전트(Codex, Claude Code)는 `/studio` 하위 UI 작업 시 이 문서를 준수한다.
여기 없는 값을 새로 만들지 말 것. 필요하면 구현하지 말고 보고할 것.

### AI 코딩 에이전트 작업 절차

Studio UI를 수정하기 전에 순서대로 수행한다.

1. 이 문서를 먼저 읽는다.
2. 지금 만지는 화면이 **List / Detail / Settings / Form** 중 무엇인지 판정한다 (§6 Page Pattern).
3. 해당 Pattern을 먼저 적용한다. 화면마다 새 레이아웃을 발명하지 않는다.
4. 기존 component·token·상수가 있으면 재사용한다.
5. 문서에 없는 레이아웃 값·색·간격을 임의로 만들지 않는다.
6. 필요한 규칙이 문서에 없으면 **구현을 중단하고 사용자에게 보고한다.**

에이전트 재량으로 추가 금지:

- KPI 카드 나열
- 장식용 레일·도트
- 중첩 카드 (card inside card)
- 새 상태 컬러
- 새 max-width
- 새 spacing 값
- 새 버튼 위계
- 대시보드형 요약 블록

---

## 0. 제품 성격 — 디자인 판단의 기준

Studio는 원장이 **매일 열어놓고 쓰는 운영 보드**다. 예쁜 화면이 아니라 빠른 판단이 목적이다.
설계가 갈릴 때 아래 순서로 판단한다.

1. **지금 처리해야 할 것이 먼저 보이는가** — 지표 나열보다 액션 유도가 우선
2. **한 정보가 한 번만 나오는가** — 통계 카드와 필터 칩이 같은 숫자를 두 번 말하면 하나를 없앤다
3. **화면에서 판단이 끝나는가** — "자세한 건 상세에서 확인" 류 문구는 그 화면이 실패했다는 뜻
4. **되돌릴 수 없는 일을 막고 있는가** — 신청이 걸린 데이터는 잠근다
5. **정보 밀도는 높게, 시각 장식은 낮게** — 원장은 하루에 수십 번 스캔한다

### 0.1 Case 중심

Studio에서 신청 한 건(`trial_applications.id`)은 신청·상담·일정이라는 서로 다른 업무가 아니라,
**등록 결과까지 이어지는 하나의 Case**다.

같은 Case를 기능별 페이지로 반복 분리하지 않는다.
"신청은 여기, 상담은 저기, 미등록은 또 저기"로 나누면 원장이 같은 학생을 세 번 찾아야 한다.

### 0.2 다음 행동 우선

상태 이름을 보여주는 것보다 **사용자가 다음에 해야 하는 행동**을 우선한다.
`체험 완료`보다 `오늘 등록 의사 확인` 이 먼저다.

시스템 내부 status transition을 사용자 action depth로 그대로 노출하지 않는다.
한 번의 업무 판단으로 끝나는 흐름이라면 중간 상태를 위한 별도 클릭 단계를 만들지 않는다.

### 0.3 동일 데이터, 다른 View

목록·캘린더·홈은 서로 다른 데이터 모델이 아니라 **같은 Case를 다른 관점에서 보는 View**다.
View마다 상태 정의나 라벨을 새로 만들지 않는다. 공용 상수·공용 파생 함수를 쓴다.

---

## 1. 앱 셸

```
┌─────────┬────────────────────────────────────┐
│ 사이드바 │  페이지 헤더 (제목·설명·주요 액션)   │
│  200px  │  ─────────────────────────────────  │
│  고정   │  툴바 (탭 / 필터 / 검색)             │
│         │  ─────────────────────────────────  │
│         │  본문 (테이블 / 카드 스택)           │
│  ─────  │                                     │
│ 학원 카드│              max 1200px             │
└─────────┴────────────────────────────────────┘
```

### 1.1 사이드바

디자인 시스템이 책임지는 것은 **메뉴 UI 규격 · active state · badge · 반응형 동작**이다.
**메뉴 구성과 순서는 이 문서가 영구 고정하지 않는다** — 현재 Studio IA를 따른다.

현재 IA (2026.09 기준):

- 대시보드
- 상담·등록
- 수업 관리
- 일정 관리
- 선생님 관리

규격:

- 폭 **200px** 고정, 배경 `--surface`, 우측 `1px solid var(--border)`.
- 상단 로고: `첫수업` (18px/800, `--brand-500`) + `파트너 센터` (11px/600, `--text-3`).
- 메뉴 항목: 13px/600, 좌측 6px 도트 인디케이터, 패딩 `8px 12px`, radius `--r-sm`.
  - 활성: `--brand-50` 배경 + `--brand-700` 텍스트 + 도트 `--brand-500`. `aria-current="page"` 필수.
  - hover: `--surface-sub` 배경.
  - **alias 경로**: 한 메뉴가 여러 route를 대표하면 active 판정을 한 곳(사이드바 컴포넌트)에서 관리한다. 화면마다 따로 판정하지 않는다.
- 하단 학원 카드: 로고 이미지(정사각, `object-fit:cover`, radius `--r-sm`) + 학원명(13px/700) + 지역(11px, `--text-3`) + 로그아웃(Text 버튼). 로고 없거나 로딩 실패 시 이니셜 폴백.

### 1.2 사이드바 배지

- 규격: 메뉴 우측 정렬, `--r-full`, 11px/700. 처리 대기는 amber, 그 외 gray.
- **0이면 숨긴다.**
- **표시 대상은 "사용자가 지금 처리해야 하는 Case 수" 하나뿐이다.**
  전체 건수, 완료 건수, 파이프라인 총합처럼 행동을 유발하지 않는 숫자는 배지로 쓰지 않는다.
  구 `상담 관리`의 pipeline count 배지는 이 이유로 제거되었다.

### 1.3 페이지 헤더

- 제목 20px/800/-0.02em, 설명 13px `--text-2` (제목 아래 4px).
- 설명은 이 화면에서 원장이 하는 일을 한 문장으로: "체험 신청을 확정하고 등록까지 흐름을 관리해요."
- **주요 액션(Primary)은 헤더 우측**에 둔다.
- 필터 배치는 위치를 강제하지 않고 **"필터를 한 곳에 모은다"** 는 원칙만 지킨다.
  - 작은 필터 세트: 헤더 하단 또는 같은 toolbar에 둘 수 있다.
  - 탭 / 필터 / 검색이 함께 있는 복잡한 경우: 헤더와 분리된 별도 toolbar로 뺄 수 있다.
  - **금지**: 같은 기능의 필터를 화면 여러 곳에 흩어놓는 것.
- 헤더 아래 `1px solid var(--border)` 구분선, 하단 여백 24px.

---

## 2. 토큰

### 2.1 컬러

```css
/* brand */
--brand-50:#EAF7EC; --brand-100:#D3EFD8;
--brand-500:#2AAD38;   /* 아이콘·인디케이터·강조 테두리. 텍스트 금지 */
--brand-700:#1B7A26;   /* 강조 텍스트·프라이머리 버튼 배경 (5.44:1) */
--brand-900:#12561A;   /* hover/pressed */

/* gray */
--text-1:#1A1A1A; --text-2:#5C5C5C; --text-3:#8E8E8E;
--bg:#F5F5F5;          /* Studio 페이지 배경 */
--surface:#FFFFFF;     /* 카드·사이드바 */
--surface-sub:#FAFAFA; /* 잠금·비활성·서브 영역 */
--border:#E8E8E8; --border-strong:#D4D4D4;

/* semantic — fg/bg 항상 쌍으로 사용 */
--green-fg:#1B7A26; --green-bg:#EAF7EC;
--amber-fg:#8A5A00; --amber-bg:#FBF3E4;
--blue-fg:#245EA8;  --blue-bg:#EEF4FC;
--gray-fg:#5C625C;  --gray-bg:#F1F2F1;
--red-fg:#B42318;   --red-bg:#FDECEC;
```

`#2AAD38`은 흰 배경 대비 2.94:1로 AA 미달 → 텍스트·버튼 배경 금지.
레거시 치환 대상: `#1E8C2B`/`#219230` → `--brand-700`, `#F2FBF3`/`#E9F8EC`/`#F3FBF4` → `--brand-50`.
캔디 액센트(노랑·하늘·핑크·크림)는 **Studio에서 사용 금지** — 브랜드/마케팅 전용.

**semantic foreground 접근성**: amber/blue/red/gray fg는 배지·12px 메타 같은 작은 텍스트에 쓰이므로
각자의 `--*-bg` 위에서 약 **4.9:1 ~ 5.9:1** 대비를 확보하도록 보정된 값이다.
밝은 fg(`#C98A1E`, `#3B7DD8`, `#E5484D`)는 v1.0 레거시이며 사용하지 않는다.
구분선·아웃라인이 필요하면 새 토큰을 만들지 말고 `var(--border)` 를 쓴다.

### 2.2 Case 상태 모델

Studio의 상태는 두 축이다. **하나로 합쳐 표현하지 않는다.**

**축 1 — `ApplicationStatus` (DB 값 5개)**

| 상태 | 라벨 | 컬러 | 성격 |
|---|---|---|---|
| `new` | 신청 | amber | 처리 대기 |
| `reviewing` | 확인 중 | blue | 진행 중 |
| `confirmed` | 일정 확정 | green | 진행 중 |
| `completed` | 체험 완료 | gray | **체험수업이 끝났다는 뜻. Case 종료가 아니다** |
| `canceled` | 취소 | red | 종료 |

**축 2 — `registrationStatus` (등록 결과)**

| 값 | 라벨 | 컬러 | 성격 |
|---|---|---|---|
| `undecided` / null | 등록 미결정 | blue | **진행 Case.** 아직 의사결정이 충분히 기록되지 않았다. "재상담"이 아니라 "지금 기록해 주세요"로 유도 |
| `pending` | 고민 중 | amber | **진행 Case.** 후속 상담 가능성이 높다 — 전환 기회 최고 |
| `enrolled` | 등록 | green | **종료 결과** |
| `not_enrolled` | 미등록 | gray | **현재 Case의 종료 결과** |

**파생 상태 (DB status 아님)**

- `no_show` — `canceled` + `no_show_at` 존재에서 파생되는 **화면용** 상태. red / 종료.
- `일정 변경` — 상태값이 아니라 활동 기록에 남는 **UI 이벤트**다. 상태 배지로 쓰지 않는다.

### 2.3 Case 진행 / 종료 판정

**진행 중인 Case**

- `new`
- `reviewing`
- `confirmed`
- `completed` + `undecided`
- `completed` + `pending`

**완료·종료된 Case**

- `completed` + `enrolled`
- `completed` + `not_enrolled`
- `canceled`
- `no_show` (파생)

`completed` 단독으로 Case를 종료로 분류하지 않는다.
`not_enrolled` 는 **이번 Case의 결론**이지 그 학생에 대한 영구 판정이 아니다.
재접촉·재활성화 기능이 생기더라도 그것은 별도의 derived state 또는 별도 기능으로 다루며,
`not_enrolled` 자체의 의미를 바꾸지 않는다.

### 2.4 타이포 · 간격 · 라운드

| 역할 | 값 |
|---|---|
| 페이지 제목 | 20px / 800 / -0.02em |
| 섹션 제목 | 16px / 700 / -0.02em |
| 행의 주인공 (학생명·슬롯 시간) | 13–15px / 800 |
| 본문 | 14px / 400–500 / lh 1.55 |
| 보조·필드 라벨 | 12–13px / 600–700 / `--text-2` |
| 배지·메타 | 11–12px / 700 |
| **숫자** | `font-variant-numeric: tabular-nums` **필수** — 표·카운트 정렬 흔들림 방지 |

**간격(padding·margin·gap)은 4px 배수만**: `--s1:4 --s2:8 --s3:12 --s4:16 --s5:20 --s6:24 --s8:32 --s10:40`.

| 용도 | 값 |
|---|---|
| 카드 패딩 | `24px` |
| compact 카드 패딩 | `16px` 또는 `20px` |
| 카드 간격 | `20px` 또는 `24px` |
| Section 내부 gap | `12px` 또는 `16px` |
| 버튼 패딩 | `8px 16px` |
| 인풋 패딩 | `8px 12px` |

라운드는 **토큰만** 사용한다. 토큰에 없는 값(7·9·10·14px 등)을 새로 쓰지 않는다.

| 대상 | 토큰 |
|---|---|
| 칩 · 작은 요소 · 스테퍼 | `--r-sm` (8px) |
| 인풋 · 버튼 · 리스트 행 · 카드 | `--r-md` (12px) |
| 큰 카드 · 페이지 카드 · 모달 | `--r-lg` (16px) |
| 배지 · 토글 | `--r-full` |

### 2.5 섀도우

**카드 무섀도우.** `1px solid var(--border)` 플랫 아웃라인.
예외: 모달, 드롭다운, 토스트, 스티키 바 — `0 8px 28px rgba(0,0,0,.07)` 수준까지.

### 2.6 Content Width

- Studio 본문 기본 폭은 **`--studio-max: 1200px`** 하나다. 이 토큰이 유일한 Source of Truth다.
- 목록·상세·설정 모두 특별한 이유가 없으면 이 값을 따른다.
- **같은 페이지의 주요 Section은 같은 width를 쓴다.**
  Detail Page의 Header / Next Action / Activity / Domain Section / Additional Information이
  서로 다른 max-width를 가지면 안 된다.
- 이유 없이 어떤 Section만 full width, 어떤 Section만 좁은 중앙 카드로 만드는 것을 **금지**한다.
- 구현은 개별 Section마다 `max-width`를 반복하지 말고 **컨테이너에서 한 번** 처리한다.

---

## 3. Page Pattern

새 화면을 만들기 전에 아래 4개 중 무엇인지 먼저 정한다.

### 3.1 List Page

```
Page Header
Tabs / Filter / Search
List / Table
Pagination
```

### 3.2 Detail Page

```
Back
Entity Header
Next Action
Activity
Domain Information
Additional Information
```

### 3.3 Settings Page

```
Page Header
Sub Navigation
Settings Sections
```

### 3.4 Form Page

```
Page Header + Save
Form
Preview (필요한 화면만)
```

---

## 4. Case Pattern

Studio의 핵심 업무 흐름. 아래 두 패턴은 **재해석 대상이 아니다.**

### 4.1 Case List Pattern — 상담·등록

```
Page Header
[진행 중] [완료·종료]
Filter / Search
Case Table
Pagination
```

**진행 중 컬럼 (최대 6개)**

학생 · 현재 단계 · 체험수업 · 다음 행동 · 담당자 · 최근 활동

**완료·종료 컬럼**

학생 · 결과 · 체험수업 · 담당자 · 종료일

**규칙**

- active와 closed에서 **의미 없는 동일 컬럼을 강제하지 않는다.** 두 표는 컬럼 수가 달라도 된다.
- 종료된 Case에 `다음 행동 —` 을 반복 출력하지 않는다.
- 다음 행동은 **일반 텍스트가 기본**이다.
  - 오늘 처리해야 하는 일 → amber
  - 기한이 지난 일 → red
  - 일반 업무를 red로 칠하지 않는다. 다 빨가면 아무것도 빨갛지 않다.
- **행 전체가 클릭 타깃**이며 Case Detail로 이동한다.
- **Pagination 기본 적용.** 25건 이상을 전체 client load한 뒤 잘라내는 방식 금지 — DB 레벨에서 페이징한다.

### 4.2 Case Detail Pattern — 상담·등록 상세

정보구조는 상태와 무관하게 **항상 같은 순서**다.

```
1. Case Header
2. 신청 정보
3. 다음 할 일
4. 활동 기록
5. Domain-specific section   (현재: 체험 결과)
```

상태가 달라져도 Section 순서를 크게 바꾸지 않는다. 상태별로 다른 화면처럼 보이면 실패다.

#### Case Header

표시 우선순위:

1. 학생 이름 + 학년
2. 현재 등록 상태 badge
3. 체험수업명 + 담당자
4. 보호자 + 연락처
5. 핵심 action (전화·문자)
6. compact progress (한 줄)

**Header를 대시보드처럼 만들지 않는다.** 지표 카드·큰 Stepper를 넣지 않는다.

#### 다음 할 일 (Next Action)

상세에서 **가장 중요한 Section**이다.

- action card는 **하나만**.
- **card inside card 금지.**
- 내용이 1~2줄이면 큰 `min-height`를 주지 않는다. 내용이 적으면 카드도 작아야 한다.
- **Primary Action 최대 1개.** 나머지는 Ghost / Text.
- 오늘 일정 → amber, 기한 초과 → red, 일반 다음 행동 → 기본 텍스트.

#### 활동 기록 (Activity)

상담 기록과 시스템 이벤트를 **같은 위계로 표현하지 않는다.**

**상담 기록 — 중요 Event (펼침)**

실제 데이터가 있는 것만: 방식(channel) · 일시 · 내용(note) · 학부모 반응 · 등록 상태 · 다음 연락.
note가 이 Section의 본문이므로 가장 큰 글자다.

**시스템 Event — compact 한 줄**

신규 신청 · 일정 확정 · 체험 완료 · 체험 결과 · 등록/미등록 · 취소/노쇼.

**없는 이력을 만들지 않는다.**
담당자 변경·일정 재변경 이력이 DB에 없으면, 현재 상태를 과거 Event처럼 추측해 Timeline에 넣지 않는다.
("현재 담당이 A" 라는 사실만으로 "8/25 A 배정" 을 만들어내지 않는다.)

#### Domain Section (현재: 체험 결과)

- **Description layout(라벨 / 값)이 기본**이다.
- 내용이 적으면 높이도 작게. 빈 칸을 채우려고 요소를 늘리지 않는다.
- **KPI Card를 넣지 않는다.**

#### 신청 정보

- Case 판단에 필요한 신청 정보는 기본 노출한다.
- 사용자가 핵심 업무를 수행하기 위해 펼침 동작을 추가로 요구하지 않는다.
- 실제 값이 있는 항목을 중심으로 표시하고, 값이 없는 필드를 `-` 행으로 반복하지 않는다.

### 4.3 Case Detail 금지 규칙

1. KPI 카드 나열
2. Card 안에 Card 반복
3. Section마다 장식용 dot 반복
4. 주요 Section마다 서로 다른 max-width
5. 한 영역에 Primary 버튼 2개 이상
6. 시스템 Event와 상담 기록을 동일 크기로 표시
7. 정보가 적은데 큰 `min-height` 사용
8. 동일 상태·숫자를 화면 여러 곳에서 반복 표시
9. 상세 정보보다 상태 Stepper가 더 강하게 보이는 구조

---

## 5. 시그니처 컴포넌트

Studio를 Studio답게 만드는 요소들. 새로 만들지 말고 이걸 재사용한다.
아래는 **현재 유지·재사용 가능한 컴포넌트**이며, 특정 구 페이지(신청 관리 / 상담 관리 / 미등록 학생관리)를
되살리라는 뜻이 아니다. IA는 §1.1을 따른다.

### 5.1 전환 파이프라인 바

통계 카드와 필터 칩의 중복을 대체하는 단일 요소. **숫자를 두 번 말하지 않는다.**

```
[신규 신청 5]→[일정 확정 3]→[체험 완료 8]→[등록 완료 2]  │ 취소 1 · 노쇼 0 · 미등록 4
                    등록 전환율 14.3%
```

- 각 스테이지는 **버튼이자 필터**. `aria-pressed`로 상태 표현, 활성 시 `--brand-50` 배경 + `--brand-500` 테두리.
- 스테이지 구성: 라벨(12px/600 `--text-2`) + 숫자(24px/800 tabular) + 보조문(11px `--text-3`, "1건 선생님 배정 필요"처럼 **행동을 지시**).
- 스테이지 사이 `›` 화살표 아이콘, `aria-hidden`.
- 우측에 종료 상태(취소·노쇼·미등록)를 구분해서 묶는다 — 퍼널이 아니라 참고값이므로 작게.
- 하단 푸터에 **등록 전환율**. 이 제품의 존재 이유이므로 항상 보이게 둔다.
- 대시보드 등 **요약이 목적인 화면에서만** 쓴다. Case 목록·상세에 KPI로 끼워 넣지 않는다.

### 5.2 캘린더 + 우측 패널 (예약시간 설정)

- 좌 1.6 : 우 1 그리드, 사이 `1px solid var(--border)` 세로 구분.
- **날짜 셀**: min-height 76px, radius `--r-md`. 내부 = 날짜(12/700) + 타임 수 pill(brand) 또는 휴무/마감 pill(gray) + 신청 건수 dot(우상단, `--text-1` 배경/흰 텍스트) + 잠금 🔒(우하단).
- 일요일 숫자 `--red-fg`. 지난 날짜 opacity 0.4 + 클릭 불가. 선택 시 `--brand-500` 테두리 + `--brand-50` ring 2px.
- **우측 패널**은 선택 날짜 하나만 다룬다. 날짜 미선택 시 빈 상태 문구.

### 5.3 슬롯 행 + 잠금 규칙

- radius `--r-md`, 시간이 13px/800으로 주인공. 정원은 `−/+` 스테퍼(24px, radius `--r-sm`).
- **신청이 있는 슬롯은 잠근다**: `--surface-sub` 배경, `--border-strong` 테두리, 배지 `신청 n건`(amber) + `🔒 수정 잠금`(gray), 삭제 버튼 자리에 "삭제 불가" 비활성 텍스트, 정원은 신청 건수 아래로 감소 불가.
- 닫힌 슬롯: opacity 0.45 + "열기" 복구 버튼(`--brand-700` 텍스트).
- 그 날짜에만 추가된 슬롯은 `이 날만` 배지(green).

### 5.4 규칙 편집기 (반복 설정)

슬롯을 한 줄씩 만들게 하지 않는다. **규칙 → 자동 생성**이 기본.

- 규칙 요약 행: 요일 도트(24px 사각, 선택 시 brand 채움) + `오후 4:00 ~ 오후 7:00 · 60분 · 정원 3 · 하루 3타임`.
- 편집 중인 규칙은 `--brand-500` 테두리 + `--brand-50` ring 3px + 배경 `--brand-50`.
- **실시간 생성 힌트 필수**: 입력이 바뀔 때마다 `→ 화·목요일마다 하루 3개 타임이 자동 생성돼요` (12px, `--brand-700`).

### 5.5 탭 위저드 (수업 등록·수정)

긴 폼은 한 화면에 쏟지 않는다.

- 상단 스텝 인디케이터 + 하단 **스티키 액션 바**(`position:fixed; left:200px; right:0`, `backdrop-filter: blur(8px)`).
- 스티키 바 구성: `임시저장`(Ghost) — 진행 안내(중앙, 12px `--text-3`) — `이전`(Ghost) / `다음`(Primary).
- 다음 버튼 비활성 시 opacity 0.35, `cursor:not-allowed`.
- 패널 전환은 `fadein .2s`. `prefers-reduced-motion` 존중.

---

## 6. 기본 컴포넌트

### 6.1 버튼

| 종류 | 스타일 | 용도 |
|---|---|---|
| Primary | `--brand-700` 배경 / 흰 텍스트 / hover `--brand-900` / radius `--r-md` / 패딩 `8px 16px` | **한 영역에 1개.** 저장, 확정 |
| Ghost | 흰 배경 / `--border` 1px / hover 시 테두리·텍스트 `--brand-700` | 추가, 취소, 보조 |
| Text | 배경 없음 / `--text-2` / hover 시 의미 색 | 삭제, 부가 |
| Danger | Ghost + `--red-fg` 텍스트, 또는 `--red-fg` 배경 | 파괴적 액션. confirm 필수 |

**검은색 버튼 금지.** 버튼 텍스트는 결과를 말한다: "저장"이 아니라 "규칙 저장", "확인"이 아니라 "일정 확정하기". 같은 액션은 흐름 내내 같은 동사.

### 6.2 카드

**Card는 아래 경우에만 쓴다.**

- 독립된 업무 단위
- 중요한 action
- 명확히 구분해야 하는 데이터 그룹

"Section이니까"라는 이유만으로 카드를 씌우지 않는다.
**Card inside Card 금지.** 정말 필요하면 코드 리뷰에서 이유를 설명해야 한다.

### 6.3 테이블 (목록형 화면)

- 6컬럼 이내로 유지. 날짜 중복 컬럼, 배정 상태 라벨 중복, slug 문자열 노출 금지 (**개발자용 문자열이 원장 화면에 나오면 안 된다**).
- 보호자명과 연락처는 한 셀에 합친다.
- **행 전체가 클릭 타깃.** 별도 액션 버튼 컬럼을 만들지 않는다.
- **처리 필요한 행은 좌측 3px `--brand-500` 레일.** 취소·노쇼 행은 opacity 0.55로 죽인다.
- 헤더 12px/700 `--text-2`, 셀 13px, 행 패딩 `12px 16px`, hover 시 `--surface-sub`.
- 900px 이하: 카드형으로 전환.

### 6.4 배지

`--r-full`, 11–12px/700, 패딩 `4px 8px`. 항상 `--*-bg` + `--*-fg` 쌍. 흰 배경 + 컬러 텍스트만 있는 배지 금지.

### 6.5 폼

- 인풋: 흰 배경, `--border` 1px, radius `--r-md`, 14px, 패딩 `8px 12px`. focus 시 `--brand-500` 테두리.
- 필드 라벨 12px/700 `--text-2`, 인풋 위 4px.
- 도움말은 인풋 아래 12px. 중립 정보는 `--text-3`, 결과 예고는 `--brand-700`.
- 에러는 `--red-fg` + 무엇이 잘못됐고 어떻게 고치는지. 사과하지 않는다.
- 토글 스위치 40×24px. on 색은 의미에 따라 — 활성화면 `--brand-700`, 차단(휴무 등)이면 `--red-fg`.

### 6.6 안내·경고 블록

- 정보 안내: `--brand-50` 배경 + `i` 아이콘 원형 + 제목(13px/700) + 본문(12px `--text-2`). 화면 상단 1개까지.
- 경고: `--amber-bg` 배경 + `--amber-fg` 텍스트, 12px, radius `--r-sm`. **조건부로만 노출** — 상시 노출되면 아무도 안 읽는다.

### 6.7 빈 상태

`--text-3`, 중앙 정렬, 2줄 이내, **다음 행동을 반드시 안내**한다.
`이 날짜에 생성된 타임이 없어요` / `아래에서 이 날짜에만 시간을 추가할 수 있어요` + 액션 버튼.
필터 결과 0건이면 `[조건 초기화]` 버튼 필수. 일러스트·이모지 남용 금지.

---

## 7. UX 라이팅

- 존댓말 해요체. 원장을 가르치지 않는다.
- 시스템 용어 금지. 사용자가 하는 일로 부른다.
- 시간은 `오후 4:00`. **초 단위가 화면에 보이면 데이터 버그로 보고할 것** (`16:11:00` 같은 값).
- 요약은 판단이 끝나게: `8/14(목) 16:00 · 이번 주 3회 가능`. `외 39개 일정` `자세한 시간은 상세에서` 금지.
- 파괴적 confirm은 결과를 구체적으로: "이 운영 시간 규칙을 삭제할까요? 앞으로 생성될 예약 타임이 사라져요."
- 잠금 안내는 이유와 우회로를 같이: "이미 신청된 예약이 있는 타임은 유지돼요. 취소가 필요하면 상담·등록에서 개별 처리해 주세요."

---

## 8. 반응형

- 기본 데스크탑. `--studio-max: 1200px`.
- **900px 이하**: 사이드바 숨김(또는 상단 접힘), 2단 그리드 → 1단 스택, 테이블 → 카드형, 스티키 바 `left:0`, 액션 버튼 가로 배치.
- 원장은 데스크탑이 주지만 현장에서 폰으로 신청 확인을 한다. **상담·등록 목록과 상세는 모바일 동작 필수 검증 대상.**

---

## 9. 접근성 최소선

- 키보드 포커스 링 항상 보이게. `outline` 제거 금지.
- 필터 버튼 `aria-pressed`, 현재 메뉴 `aria-current="page"`, 장식 아이콘 `aria-hidden`.
- `@media (prefers-reduced-motion: reduce) { *{transition:none!important} }` 포함.
- 상태를 **색으로만** 전달하지 않는다. 배지에 텍스트를 같이 넣는다.
- semantic fg는 §2.1의 보정값을 쓴다. 작은 텍스트에 밝은 레거시 색을 되살리지 않는다.

---

## 10. 구독 / Entitlement 원칙

향후 구독 티어가 들어올 예정이지만 **현재 plan infrastructure는 없다.**
이 문서는 구조 원칙만 정의하며, 실제 entitlement를 지금 구현하지 않는다.

### 10.1 동일 Studio, 기능 깊이만 차등

- 플랜마다 서로 다른 IA나 서로 다른 Studio 레이아웃을 만들지 않는다.
- Core UX는 모든 플랜에서 동일하다.
- Feature entitlement에 따라 **기능 활성화 / advanced feature / automation / analytics의 깊이만** 차등한다.

### 10.2 잠금 UI

- 잠긴 기능은 **사용자의 업무 맥락 안에서** 안내한다.
- 사이드바에 잠긴 메뉴를 대량으로 추가하지 않는다.
- **UI-only gate 금지.** 숨기는 것은 보안이 아니다.
- **server entitlement 검증 필수.**

---

## 11. 금지 목록

1. 여기 없는 색·간격·크기를 새로 만들지 않는다. 필요하면 구현 말고 보고.
2. `#2AAD38`을 텍스트·버튼 배경에 사용 금지.
3. 검은색 버튼 금지.
4. 카드 box-shadow 금지 (모달·드롭다운·토스트·스티키 바 제외).
5. 4px 배수 아닌 간격 금지. 토큰에 없는 radius 금지.
6. 캔디 액센트 Studio 사용 금지.
7. 상태 라벨 하드코딩 금지 — 공용 상수 확장.
8. 인라인 스타일 금지 — module.css + 토큰.
9. 같은 숫자를 한 화면에서 두 번 표시 금지.
10. slug·id·enum 원문 등 개발자용 문자열 화면 노출 금지.
11. 신청이 존재하는 슬롯·일정의 삭제/축소 UI 노출 금지 — 잠금 처리.
12. 기존 컴포넌트·상수가 있으면 새로 만들지 말고 확장.
13. 같은 페이지의 주요 Section에 서로 다른 max-width 금지.
14. Card inside Card 금지.
15. Case Detail에 KPI 카드 금지.
16. DB에 없는 이력을 Timeline에 추측으로 생성 금지.

---

## 12. 토큰 블록 (globals.css)

```css
:root{
  --brand-50:#EAF7EC;
  --brand-100:#D3EFD8;
  --brand-500:#2AAD38;
  --brand-700:#1B7A26;
  --brand-900:#12561A;

  --text-1:#1A1A1A;
  --text-2:#5C5C5C;
  --text-3:#8E8E8E;

  --bg:#F5F5F5;
  --surface:#FFFFFF;
  --surface-sub:#FAFAFA;

  --border:#E8E8E8;
  --border-strong:#D4D4D4;

  --green-fg:#1B7A26;
  --green-bg:#EAF7EC;

  --amber-fg:#8A5A00;
  --amber-bg:#FBF3E4;

  --blue-fg:#245EA8;
  --blue-bg:#EEF4FC;

  --gray-fg:#5C625C;
  --gray-bg:#F1F2F1;

  --red-fg:#B42318;
  --red-bg:#FDECEC;

  --r-sm:8px;
  --r-md:12px;
  --r-lg:16px;
  --r-full:999px;

  --s1:4px;
  --s2:8px;
  --s3:12px;
  --s4:16px;
  --s5:20px;
  --s6:24px;
  --s8:32px;
  --s10:40px;

  --studio-max:1200px;
  --side-w:200px;
}
```

---

## 13. 브랜드 — 변경하지 않는 것

- 첫수업 로고
- 현재 Studio 폰트
- 첫수업 Key Green 계열
- 브랜드 이름
- 전체적으로 밝고 차분한 SaaS 성격

v1.1은 특정 외부 제품(Wise 등)의 디자인을 복제하는 작업이 **아니다.**
참고하는 것은 원칙뿐이다: hierarchy · density · component consistency · action clarity ·
neutral surface usage · restrained decoration.

---

*v1.1 — 2026.09. Studio IA를 Case 중심(대시보드 · 상담·등록 · 수업 관리 · 일정 관리 · 선생님 관리)으로 개편하면서 갱신.
`trial_applications.id` = 하나의 Case라는 전제 위에 Case List / Case Detail Pattern, Content Width 규칙,
Detail 금지 규칙, 구독 원칙, AI 에이전트 작업 절차를 추가했다.
v1.0에 남아 있던 구 본문 폭, 토큰에 없는 구분선 변수, 토큰 밖 radius, 4px 배수가 아닌 카드 패딩 등
서로 충돌하던 값은 모두 제거하고 토큰 기준으로 통일했다.
변경 시 이 문서를 먼저 고치고 코드에 반영한다.*
