# 첫수업 Studio 디자인 시스템 v2

파트너 센터(`/studio`) 전용 기준 문서. 학원 운영자가 쓰는 **업무 도구**의 규칙이다.
학부모 페이지(`/classes`, `/my`)에는 적용하지 않는다 — 그쪽은 480px 모바일 컬럼 체계로 별도 관리.

AI 코딩 에이전트(Codex, Claude Code)는 `/studio` 하위 UI 작업 시 이 문서를 준수한다.
여기 없는 값을 새로 만들지 말 것. 필요하면 구현하지 말고 보고할 것.

## Design Direction v2

> **넓고 정돈된 SaaS 작업 공간의 구조를 빌려오되, 화면의 주인공은 첫수업의 Green 브랜드와
> 상담 → 체험 → 등록으로 이어지는 원장의 업무 흐름이다.**

### 외부 레퍼런스(Base)의 위치

v2는 외부 SaaS 템플릿 **Base**의 화면들(Dashboard / List / Calendar / Board / Drawer /
Modal / Split Form / Auth 등)을 검토해 도출했다.

Base는 **visual clone target 이 아니다.** 참고하는 것은 아래뿐이다.

- 정보 위계
- page composition
- workspace density
- interaction pattern
- overlay 사용 방식
- list / calendar / form 구조

**가져오지 않는 것**: Base 로고 · 보라색 primary · Base 타이포그래피 · 일러스트 스타일 ·
Upgrade Now 카드 · 커머스형 KPI · 제품/매출 중심 IA · 장식 차트 · 모든 row 를 카드로 띄우는 방식 ·
불필요한 아바타 장식.

첫수업은 **기존 로고 · 기존 폰트 · 기존 Green · 기존 semantic tone · 현재 Studio IA** 를 그대로 유지한다.
v2 는 기존 Foundation(UI-1A~1E)을 폐기하는 redesign 이 아니라, 그 위에 **화면 문법**을 얹는 작업이다.

### AI 코딩 에이전트 작업 절차

Studio UI를 수정하기 전에 순서대로 수행한다.

1. 이 문서를 먼저 읽는다.
2. 지금 만지는 화면이 **List / Detail / Settings / Form** 중 무엇인지 판정한다 (§6 Page Pattern).
3. 해당 Pattern을 먼저 적용한다. 화면마다 새 레이아웃을 발명하지 않는다.
4. 기존 component·token·상수가 있으면 재사용한다.
5. 문서에 없는 레이아웃 값·색·간격을 임의로 만들지 않는다.
6. 필요한 규칙이 문서에 없으면 **구현을 중단하고 사용자에게 보고한다.**

에이전트 재량으로 추가 금지:

- KPI 카드 나열
- 장식용 레일·도트
- 중첩 카드 (card inside card)
- 새 상태 컬러
- 새 max-width
- 새 spacing 값
- 새 버튼 위계
- 대시보드형 요약 블록

---

## 0. 제품 성격 — 디자인 판단의 기준

Studio는 원장이 **매일 열어놓고 쓰는 운영 보드**다. 예쁜 화면이 아니라 빠른 판단이 목적이다.
설계가 갈릴 때 아래 순서로 판단한다.

1. **지금 처리해야 할 것이 먼저 보이는가** — 지표 나열보다 액션 유도가 우선
2. **한 정보가 한 번만 나오는가** — 통계 카드와 필터 칩이 같은 숫자를 두 번 말하면 하나를 없앤다
3. **화면에서 판단이 끝나는가** — "자세한 건 상세에서 확인" 류 문구는 그 화면이 실패했다는 뜻
4. **되돌릴 수 없는 일을 막고 있는가** — 신청이 걸린 데이터는 잠근다
5. **정보 밀도는 높게, 시각 장식은 낮게** — 원장은 하루에 수십 번 스캔한다

### 0.1 Case 중심

Studio에서 신청 한 건(`trial_applications.id`)은 신청·상담·일정이라는 서로 다른 업무가 아니라,
**등록 결과까지 이어지는 하나의 Case**다.

같은 Case를 기능별 페이지로 반복 분리하지 않는다.
"신청은 여기, 상담은 저기, 미등록은 또 저기"로 나누면 원장이 같은 학생을 세 번 찾아야 한다.

### 0.2 다음 행동 우선

상태 이름을 보여주는 것보다 **사용자가 다음에 해야 하는 행동**을 우선한다.
`체험 완료`보다 `오늘 등록 의사 확인` 이 먼저다.

시스템 내부 status transition을 사용자 action depth로 그대로 노출하지 않는다.
한 번의 업무 판단으로 끝나는 흐름이라면 중간 상태를 위한 별도 클릭 단계를 만들지 않는다.

### 0.3 Workspace 우선

Studio 는 **사이드바 + 넓은 Main Workspace** 구조다.
Main 은 `--bg` 위에 놓이고, `--surface` 는 **필요한 영역에만** 쓴다.

**모든 내용을 Card 안에 넣지 않는다.** 표·폼·목록은 카드로 감싸지 않아도 되는 것이 기본이고,
위계는 `surface + border + spacing` 으로 만든다.

### 0.4 정돈됨 ≠ 저밀도

넓고 밝은 인상을 위해 정보량을 줄이지 않는다.
`상담·등록 목록`, `일정 목록` 같은 화면은 **한 화면에서 충분한 행을 볼 수 있는가** 가 더 중요하다.
예쁘게 보이려고 row height · padding · card gap 을 키우지 않는다.

### 0.5 동일 데이터, 다른 View

목록·캘린더·홈은 서로 다른 데이터 모델이 아니라 **같은 Case를 다른 관점에서 보는 View**다.
View마다 상태 정의나 라벨을 새로 만들지 않는다. 공용 상수·공용 파생 함수를 쓴다.

---

## 1. 앱 셸

```
┌─────────┬────────────────────────────────────┐
│ 사이드바 │  페이지 헤더 (제목·설명·주요 액션)   │
│  260px  │  ─────────────────────────────────  │
│  고정   │  툴바 (탭 / 필터 / 검색)             │
│         │  ─────────────────────────────────  │
│         │  본문 (테이블 / 카드 스택)           │
│  ─────  │                                     │
│ 학원 카드│              max 1200px             │
└─────────┴────────────────────────────────────┘
```

### 1.1 사이드바

디자인 시스템이 책임지는 것은 **메뉴 UI 규격 · active state · badge · 반응형 동작**이다.
**메뉴 구성과 순서는 이 문서가 영구 고정하지 않는다** — 현재 Studio IA를 따른다.

현재 IA (2026.09 기준):

- 대시보드
- 상담·등록
- 수업 관리
- 일정 관리
- 선생님 관리

규격:

- 폭 **260px** 고정(`--side-w`), 배경 `--surface`, 우측 `1px solid var(--border)`.
  - 840px 이하에서는 220px 로 좁아진다. 이 축소값은 별도 토큰으로 만들지 않고 반응형 규칙으로만 둔다.
- 상단 로고: `첫수업` (18px/800, `--brand-500`) + `파트너 센터` (11px/600, `--text-3`).
- 메뉴 항목: 13px/600, 좌측 6px 도트 인디케이터, 패딩 `8px 12px`, radius `--r-sm`.
  - 활성: `--brand-50` 배경 + `--brand-700` 텍스트 + 도트 `--brand-500`. `aria-current="page"` 필수.
  - hover: `--surface-sub` 배경.
  - **alias 경로**: 한 메뉴가 여러 route를 대표하면 active 판정을 한 곳(사이드바 컴포넌트)에서 관리한다. 화면마다 따로 판정하지 않는다.
- 하단 학원 카드: 로고 이미지(정사각, `object-fit:cover`, radius `--r-sm`) + 학원명(13px/700) + 지역(11px, `--text-3`) + 로그아웃(Text 버튼). 로고 없거나 로딩 실패 시 이니셜 폴백.

### 1.2 사이드바 배지

- 규격: 메뉴 우측 정렬, `--r-full`, 11px/700. 처리 대기는 amber, 그 외 gray.
- **0이면 숨긴다.**
- **표시 대상은 "사용자가 지금 처리해야 하는 Case 수" 하나뿐이다.**
  전체 건수, 완료 건수, 파이프라인 총합처럼 행동을 유발하지 않는 숫자는 배지로 쓰지 않는다.
  구 `상담 관리`의 pipeline count 배지는 이 이유로 제거되었다.

### 1.3 페이지 헤더

- 제목 20px/800/-0.02em, 설명 13px `--text-2` (제목 아래 4px).
- 설명은 이 화면에서 원장이 하는 일을 한 문장으로: "체험 신청을 확정하고 등록까지 흐름을 관리해요."
- **주요 액션(Primary)은 헤더 우측**에 둔다.
- 필터 배치는 위치를 강제하지 않고 **"필터를 한 곳에 모은다"** 는 원칙만 지킨다.
  - 작은 필터 세트: 헤더 하단 또는 같은 toolbar에 둘 수 있다.
  - 탭 / 필터 / 검색이 함께 있는 복잡한 경우: 헤더와 분리된 별도 toolbar로 뺄 수 있다.
  - **금지**: 같은 기능의 필터를 화면 여러 곳에 흩어놓는 것.
- 헤더 아래 `1px solid var(--border)` 구분선, 하단 여백 24px.

---

## 2. 토큰

### 2.1 컬러

```css
/* brand */
--brand-50:#EAF7EC; --brand-100:#D3EFD8;
--brand-500:#2AAD38;   /* 아이콘·인디케이터·강조 테두리. 텍스트 금지 */
--brand-700:#1B7A26;   /* 강조 텍스트·프라이머리 버튼 배경 (5.44:1) */
--brand-900:#12561A;   /* hover/pressed */

/* gray */
--text-1:#1A1A1A; --text-2:#5C5C5C; --text-3:#8E8E8E;
--bg:#F5F5F5;          /* Studio 페이지 배경 */
--surface:#FFFFFF;     /* 카드·사이드바 */
--surface-sub:#FAFAFA; /* 잠금·비활성·서브 영역 */
--border:#E8E8E8; --border-strong:#D4D4D4;

/* semantic — fg/bg 항상 쌍으로 사용 */
--green-fg:#1B7A26; --green-bg:#EAF7EC;
--amber-fg:#8A5A00; --amber-bg:#FBF3E4;
--blue-fg:#245EA8;  --blue-bg:#EEF4FC;
--gray-fg:#5C625C;  --gray-bg:#F1F2F1;
--red-fg:#B42318;   --red-bg:#FDECEC;
```

`#2AAD38`은 흰 배경 대비 2.94:1로 AA 미달 → 텍스트·버튼 배경 금지.
레거시 치환 대상: `#1E8C2B`/`#219230` → `--brand-700`, `#F2FBF3`/`#E9F8EC`/`#F3FBF4` → `--brand-50`.
캔디 액센트(노랑·하늘·핑크·크림)는 **Studio에서 사용 금지** — 브랜드/마케팅 전용.

**semantic foreground 접근성**: amber/blue/red/gray fg는 배지·12px 메타 같은 작은 텍스트에 쓰이므로
각자의 `--*-bg` 위에서 약 **4.9:1 ~ 5.9:1** 대비를 확보하도록 보정된 값이다.
밝은 fg(`#C98A1E`, `#3B7DD8`, `#E5484D`)는 v1.0 레거시이며 사용하지 않는다.
구분선·아웃라인이 필요하면 새 토큰을 만들지 말고 `var(--border)` 를 쓴다.

### 2.2 Case 상태 모델

Studio의 상태는 두 축이다. **하나로 합쳐 표현하지 않는다.**

**축 1 — `ApplicationStatus` (DB 값 5개)**

| 상태 | 라벨 | 컬러 | 성격 |
|---|---|---|---|
| `new` | 신청 | amber | 처리 대기 |
| `reviewing` | 신청 확인 | blue | 신청 내용을 확인한 뒤의 상태. **`신청 확인` 액션으로만 진입한다** |
| `confirmed` | 일정 확정 | green | 체험 **시작 전** |
| `completed` | 체험 완료 | gray | **원장이 명시적으로 완료 처리했다는 뜻. Case 종료가 아니다** |
| `canceled` | 취소 | red | 종료 |

**축 2 — `registrationStatus` (등록 결과)**

| 값 | 라벨 | 컬러 | 성격 |
|---|---|---|---|
| `undecided` / null | 등록 미결정 | blue | **진행 Case.** 아직 의사결정이 충분히 기록되지 않았다. "재상담"이 아니라 "지금 기록해 주세요"로 유도 |
| `pending` | 고민 중 | blue | **진행 Case.** 후속 상담 가능성이 높다 — 전환 기회 최고. `undecided` 와 함께 "결정 대기" 로 묶여 같은 blue 를 쓴다 |
| `enrolled` | 등록 | green | **종료 결과** |
| `not_enrolled` | 미등록 | gray | **현재 Case의 종료 결과** |

**상태 전이 — 진행 축**

`new` → `reviewing` → `confirmed` → `completed`

- 각 전이는 **명시적인 액션 클릭으로만** 일어난다. 상세 화면을 여는 것만으로 상태가 바뀌지 않는다.
- `new` 의 유일한 진행 액션은 **`신청 확인`** 이다. `new` 에서 곧바로 `일정 확정` 으로 건너뛰지 않는다.
- `reviewing` 의 진행 액션은 **`일정 확정`** 이다.
- `canceled` 는 `new` / `reviewing` / `confirmed` 어디서든 갈 수 있다.

**파생 상태 (DB status 아님)**

| 상태 | 라벨 | 컬러 | 성격 |
|---|---|---|---|
| `in_trial` | 체험 중 | **blue** | `confirmed` + 시작 시각 도달. **종료 시각이 지나도 계속 `체험 중`이다** |
| `no_show` | 노쇼 | red | `canceled` + `no_show_at` 존재. 종료 |

- `in_trial` 은 **화면 전용**이다. DB enum 을 늘리지 않는다 — 진행 축은 여전히 `new / reviewing / confirmed / completed` 4개다.
- **시간은 `체험 완료`를 만들지 않는다.** `체험 완료`는 "원장이 체험이 끝났음을 확인하고 결과·등록 상담으로 넘겼다"는 뜻이라 명시적 액션으로만 진입한다. 즉 `schedule end ≠ completed`.
- 종료 시각은 배지를 바꾸지 않고 **다음 행동 문구**에만 쓴다.
  - 시작~종료 사이 → `체험이 진행 중입니다.`
  - 종료 이후 → `체험이 끝났다면 완료 처리해 주세요.` + `[체험 완료]` 버튼
- **blue 를 쓰는 이유**: `체험 중` 은 경고가 아니라 정상 진행이라 amber/red 를 쓸 수 없고,
  `일정 확정`(green)과 한눈에 구분되어야 한다. blue 는 이미 `reviewing` 처럼
  **진행 축의 중간 상태**에 쓰고 있어 의미가 어긋나지 않는다. 새 색을 만들지 않는다.
- `일정 변경` — 상태값이 아니라 활동 기록에 남는 **UI 이벤트**다. 상태 배지로 쓰지 않는다.

### 2.3 Case 진행 / 종료 판정

**진행 중인 Case**

- `new`
- `reviewing`
- `confirmed`
- `completed` + `undecided`
- `completed` + `pending`

**완료·종료된 Case**

- `completed` + `enrolled`
- `completed` + `not_enrolled`
- `canceled`
- `no_show` (파생)

`completed` 단독으로 Case를 종료로 분류하지 않는다.
`not_enrolled` 는 **이번 Case의 결론**이지 그 학생에 대한 영구 판정이 아니다.
재접촉·재활성화 기능이 생기더라도 그것은 별도의 derived state 또는 별도 기능으로 다루며,
`not_enrolled` 자체의 의미를 바꾸지 않는다.

### 2.4 타이포 · 간격 · 라운드

| 역할 | 값 |
|---|---|
| 페이지 제목 | 20px / 800 / -0.02em |
| 섹션 제목 | 16px / 700 / -0.02em |
| 행의 주인공 (학생명·슬롯 시간) | 13–15px / 800 |
| 본문 | 14px / 400–500 / lh 1.55 |
| 보조·필드 라벨 | 12–13px / 600–700 / `--text-2` |
| 배지·메타 | 11–12px / 700 |
| **숫자** | `font-variant-numeric: tabular-nums` **필수** — 표·카운트 정렬 흔들림 방지 |

**간격(padding·margin·gap)은 4px 배수만**: `--s1:4 --s2:8 --s3:12 --s4:16 --s5:20 --s6:24 --s8:32 --s10:40`.

| 용도 | 값 |
|---|---|
| 카드 패딩 | `24px` |
| compact 카드 패딩 | `16px` 또는 `20px` |
| 카드 간격 | `20px` 또는 `24px` |
| Section 내부 gap | `12px` 또는 `16px` |
| 버튼 패딩 | `8px 16px` |
| 인풋 패딩 | `8px 12px` |

라운드는 **토큰만** 사용한다. 토큰에 없는 값(7·9·10·14px 등)을 새로 쓰지 않는다.

| 대상 | 토큰 |
|---|---|
| 칩 · 작은 요소 · 스테퍼 | `--r-sm` (8px) |
| 인풋 · 버튼 · 리스트 행 · 카드 | `--r-md` (12px) |
| 큰 카드 · 페이지 카드 · 모달 | `--r-lg` (16px) |
| 배지 · 토글 | `--r-full` |

### 2.5 섀도우

**카드 무섀도우.** `1px solid var(--border)` 플랫 아웃라인.
일반 카드의 위계는 `surface + border + spacing` 으로 만든다.

예외 — **실제로 다른 레이어 위에 뜨는 것만**: 모달, 드로어, 드롭다운, 토스트, 스티키/플로팅 바,
포커스 링. `0 8px 28px rgba(0,0,0,.07)` 수준까지.

### 2.6 Content Width

- Studio 본문 기본 폭은 **`--studio-max: 1200px`** 하나다. 이 토큰이 유일한 Source of Truth다.
- 목록·상세·설정 모두 특별한 이유가 없으면 이 값을 따른다.
- **같은 페이지의 주요 Section은 같은 width를 쓴다.**
  Detail Page의 Header / Next Action / Activity / Domain Section / Additional Information이
  서로 다른 max-width를 가지면 안 된다.
- 이유 없이 어떤 Section만 full width, 어떤 Section만 좁은 중앙 카드로 만드는 것을 **금지**한다.
- 구현은 개별 Section마다 `max-width`를 반복하지 말고 **컨테이너에서 한 번** 처리한다.

---

## 3. Page Pattern

새 화면을 만들기 전에 **아래 9개 중 무엇인지 먼저 정한다.**
화면마다 같은 카드 조합을 복사하지 않고, 업무 유형에 맞는 패턴을 고른다.

| # | Pattern | 언제 쓰는가 | 상태 |
|---|---|---|---|
| 1 | List Page | 많은 항목을 빠르게 훑고 하나를 고른다 | 사용 중 |
| 2 | Detail Page | 한 대상의 현재 상황과 다음 행동을 다룬다 | 사용 중 |
| 3 | List + Context Panel | 목록을 떠나지 않고 선택 항목을 확인한다 | **Future** |
| 4 | Full-page Form | 여러 판단·여러 섹션이 있는 생성/수정 | 사용 중 |
| 5 | Split Form + Preview | 입력 결과를 실제 산출물로 확인해야 한다 | **Future** |
| 6 | Calendar Workspace | 시간축이 주인공인 운영 화면 | 개선 예정 |
| 7 | Board View | 단계 이동이 주 업무일 때 | **Future** |
| 8 | Dashboard | 오늘 처리할 일을 고른다 | 개선 예정 |
| 9 | Settings / Form Page | 설정값을 바꾼다 | 사용 중 |

### 3.1 List Page

```
Page Header
Tabs / Filter / Search
List / Table
Pagination
```

적용: `/studio/cases`, 수업 관리, 선생님 관리.

- 페이지 제목은 짧게. 필터·검색은 **목록 가까이** 둔다.
- 표/목록 주변에 카드를 겹치지 않는다.
- **각 row 를 떠 있는 카드로 만들지 않는다.** 행은 행이다.
- 상태는 soft semantic 배지(§6.4).
- 핵심 action 만 밖에 노출하고, 나머지는 `···` 컨텍스트 메뉴(§6.8).
- row 간격은 업무 효율을 해치지 않는 선에서 유지한다(§0.4).

### 3.2 Detail Page

```
Back
Entity Header
Next Action
Activity
Domain Information
Additional Information
```

적용: `/studio/applications/[id]`.

**현재 상황 → 다음 행동 → 핵심 정보 → 사람이 남긴 기록 → 시스템 기록** 순서가 명확해야 한다.
일반 Detail 에 KPI 카드를 넣지 않으며, 사람의 활동이 시스템 이벤트보다 크게 보여야 한다(§4.2).

### 3.3 List + Context Panel *(Future)*

```
[ List ............................ | Context Panel ]
```

목록을 떠나지 않고 선택한 항목의 핵심만 확인하는 구조.
**Full Detail 의 대체물이 아니다.** 패널에는 identity · 상태 · 핵심 metadata · 최근 정보 ·
대표 action 정도만 둔다. 편집과 워크플로는 Full Detail 로 보낸다.

후보: Case quick preview, 선생님 상세, 수업 quick info. **이번 단계에서 구현하지 않는다.**

### 3.4 Full-page Form

```
Page Header (+ 단계)
Section …
최종 확인
```

적용: `/studio/classes/new`.

여러 섹션 · 여러 판단 · 단계가 있는 업무는 Full Page 다.
**모든 폼을 Drawer 로 바꾸지 않는다.** 한 영역의 Primary CTA 는 최대 1개(§6.1).

### 3.5 Split Form + Preview *(Future)*

```
[ 입력 ............ | 실제 결과 Preview ]
```

왼쪽은 입력, 오른쪽은 **사용자가 실제로 보게 될 결과물**이다.
Preview 가 장식이면 이 패턴을 쓰지 않는다.

후보: 체험수업 평가 리포트, 학부모 전달용 리포트, 등록 전환 인포그래픽, 공개 수업 미리보기.

### 3.6 Calendar Workspace

```
[ 보조 패널 | 캘린더 캔버스 ]
```

왼쪽 보조 패널 후보: 미니 캘린더, 선생님 필터, 수업 필터, 일정 상태 필터.
오른쪽 캔버스: **Day / Week / Month** 우선순위로 만든다.

- Year View 는 실제 운영 필요가 확인되기 전까지 만들지 않는다.
- 일정 데이터의 주인공은 **체험수업 / 레벨테스트 운영 일정**이다.
- 일반 개인 캘린더 앱으로 확장하지 않는다(§12).
- 시간축(Timeline) 표현은 Day / Week 안에서 쓴다.

### 3.7 Board View *(Future)*

단계 이동 자체가 주 업무일 때만 고려한다.
Case 기준 후보 컬럼: 신규 신청 · 상담·확인 · 일정 확정 · 체험 후 관리.

**한 기능에 List / Board / Timeline 을 전부 넣지 않는다.**
상담·등록의 Primary View 는 **List** 이며 Board 는 optional 후보다.
Timeline 을 상담·등록에 억지로 넣지 않는다.

### 3.8 Dashboard

첫수업 Dashboard 는 **Analytics-first 가 아니라 Action-first** 다.
첫 질문은 언제나 **"오늘 무엇을 처리해야 하는가?"** 다.

상단(우선순위 영역) 후보:

- 신규 신청
- 오늘 연락 필요
- 오늘 체험수업
- 후속 상담 지연

그 아래에 신청 추이 · 체험 전환 · 등록 전환 같은 운영 지표를 둘 수 있다.
**KPI 카드 4개를 만드는 것이 Dashboard 의 목적이 되어서는 안 된다.**

차트는 다음을 모두 만족할 때만 쓴다: 실제 의사결정에 도움 · 추세/비교가 텍스트보다 명확 ·
데이터가 충분히 신뢰 가능. 한 카드에는 하나의 질문만 담고,
그라디언트 · 다색 · 장식용 도넛은 쓰지 않으며, 모든 series 를 첫수업 Green 으로 칠하지 않는다.

### 3.9 Settings / Form Page

```
Page Header
Sub Navigation
Settings Sections
```

---

## 4. Case Pattern

Studio의 핵심 업무 흐름. 아래 두 패턴은 **재해석 대상이 아니다.**

### 4.1 Case List Pattern — 상담·등록

```
Page Header
[진행 중] [완료·종료]
Filter / Search
Case Table
Pagination
```

**진행 중 컬럼 (최대 6개)**

학생 · 현재 단계 · 체험수업 · 다음 행동 · 담당자 · 최근 활동

**완료·종료 컬럼**

학생 · 결과 · 체험수업 · 담당자 · 종료일

**규칙**

- active와 closed에서 **의미 없는 동일 컬럼을 강제하지 않는다.** 두 표는 컬럼 수가 달라도 된다.
- 종료된 Case에 `다음 행동 —` 을 반복 출력하지 않는다.
- 다음 행동은 **일반 텍스트가 기본**이다.
  - 오늘 처리해야 하는 일 → amber
  - 기한이 지난 일 → red
  - 일반 업무를 red로 칠하지 않는다. 다 빨가면 아무것도 빨갛지 않다.
- **단계 배지 색은 화면마다 다시 정하지 않는다.** `getCaseStageTone` 하나만 쓴다
  (진행 축 = `STUDIO_APPLICATION_STATUS_TONES`, 등록 결과 = `STUDIO_REGISTRATION_STATUS_TONES`).
  목록이라는 이유로 로컬 매핑을 새로 만들면 같은 Case가 목록과 상세에서 다른 색이 된다.
- **"종료됐다"는 이유만으로 전부 gray로 칠하지 않는다.**
  - `취소` / `노쇼` — 예외 종료다. **red 를 유지한다.** 목록에서 바로 눈에 띄어야 한다.
  - `등록`(green) / `미등록`(gray) — 정상 종료다. 각자의 등록 결과 tone 을 쓴다.
- **진행 중 필터는 서로 겹치지 않고, 합치면 진행 중 전체와 같다.**
  `신규 신청` · `신청 확인` · `일정 확정` · `체험 후 관리`.
  목록은 DB에서 페이징하므로 **필터도 DB 레벨에서 건다** — 가져온 뒤 걸러내지 않는다.
- **`체험 중`은 배지에만 있고 필터로는 두지 않는다.**
  배지의 시작 시각은 `resolveTrialStartAtMs`(`confirmed_block.start_at` → `confirmed_slot_at`)이고,
  이 의미는 PostgREST 질의로 정확히 표현할 수 없다(논리식에서 embed 컬럼 참조 불가,
  `!inner` 로 걸면 확정 블록이 없는 `confirmed` Case가 목록에서 사라진다).
  **배지와 다른 기준으로 근사한 필터는 만들지 않는다.**
  `confirmed_slot_at`만 보는 필터를 다시 추가하는 것도 금지한다.
- **행 전체가 클릭 타깃**이며 Case Detail로 이동한다.
- **Pagination 기본 적용.** 25건 이상을 전체 client load한 뒤 잘라내는 방식 금지 — DB 레벨에서 페이징한다.

### 4.2 Case Detail Pattern — 상담·등록 상세

정보구조는 상태와 무관하게 **항상 같은 순서**다.

```
1. Case Header
2. 다음 할 일
3. 신청 정보
4. 담당 선생님
5. 활동 기록
6. Domain-specific section   (현재: 체험 결과 → 등록 상담)
```

상태가 달라져도 Section 순서를 크게 바꾸지 않는다. 상태별로 다른 화면처럼 보이면 실패다.

**`다음 할 일` 이 참조 정보보다 먼저 온다.** 원장이 상세로 들어오는 이유는
"이 학생에게 지금 무엇을 해야 하는가" 하나다. 신청 정보와 담당 선생님은
이미 알고 있거나 한 번 확인하면 끝나는 참조값이라 그 아래에 둔다.
모바일에서 첫 화면에 참조 정보가 먼저 보이면 실패다.

#### Case Header

표시 우선순위:

1. 학생 이름 + 학년
2. 현재 등록 상태 badge — **`completed` 이후에만 표시한다.**
   등록 축은 체험을 마친 뒤부터 의미가 생긴다. 체험 전 Case에 `등록 미결정` 을
   띄우면 진행 축 badge 와 경쟁하기만 하고 알려주는 것이 없다.
3. 체험수업명 + 담당자
4. 보호자 + 연락처
5. 핵심 action (전화·문자)
6. compact progress (한 줄)

**Header를 대시보드처럼 만들지 않는다.** 지표 카드·큰 Stepper를 넣지 않는다.

#### Domain-specific section 노출

`체험 결과` 와 `등록 상담` 은 **실제 `status === "completed"` 일 때만 렌더한다.**

- 파생 `체험 중` 으로는 열지 않는다. 시간이 아니라 원장의 완료 처리가 기준이다.
- 아직 열 수 없다는 안내만 담은 빈 카드를 미리 만들지 않는다.
  할 수 없는 일을 위한 자리를 화면에 남겨두지 않는다.

#### 다음 할 일 (Next Action)

상세에서 **가장 중요한 Section**이다.

- action card는 **하나만**.
- **card inside card 금지.**
- 내용이 1~2줄이면 큰 `min-height`를 주지 않는다. 내용이 적으면 카드도 작아야 한다.
- **Primary Action 최대 1개.** 나머지는 Ghost / Text.
- 오늘 일정 → amber, 기한 초과 → red, 일반 다음 행동 → 기본 텍스트.

#### 활동 기록 (Activity)

상담 기록과 시스템 이벤트를 **같은 위계로 표현하지 않는다.**

**상담 기록 — 중요 Event (펼침)**

실제 데이터가 있는 것만: 방식(channel) · 일시 · 내용(note) · 학부모 반응 · 등록 상태 · 다음 연락.
note가 이 Section의 본문이므로 가장 큰 글자다.

**시스템 Event — compact 한 줄**

신규 신청 · 일정 확정 · 체험 완료 · 체험 결과 · 등록/미등록 · 취소/노쇼.

**없는 이력을 만들지 않는다.**
담당자 변경·일정 재변경 이력이 DB에 없으면, 현재 상태를 과거 Event처럼 추측해 Timeline에 넣지 않는다.
("현재 담당이 A" 라는 사실만으로 "8/25 A 배정" 을 만들어내지 않는다.)

#### Domain Section (현재: 체험 결과)

- **Description layout(라벨 / 값)이 기본**이다.
- 내용이 적으면 높이도 작게. 빈 칸을 채우려고 요소를 늘리지 않는다.
- **KPI Card를 넣지 않는다.**

#### 신청 정보

- Case 판단에 필요한 신청 정보는 기본 노출한다.
- 사용자가 핵심 업무를 수행하기 위해 펼침 동작을 추가로 요구하지 않는다.
- 실제 값이 있는 항목을 중심으로 표시하고, 값이 없는 필드를 `-` 행으로 반복하지 않는다.

### 4.3 Case Detail 금지 규칙

1. KPI 카드 나열
2. Card 안에 Card 반복
3. Section마다 장식용 dot 반복
4. 주요 Section마다 서로 다른 max-width
5. 한 영역에 Primary 버튼 2개 이상
6. 시스템 Event와 상담 기록을 동일 크기로 표시
7. 정보가 적은데 큰 `min-height` 사용
8. 동일 상태·숫자를 화면 여러 곳에서 반복 표시
9. 상세 정보보다 상태 Stepper가 더 강하게 보이는 구조

---

## 5. 시그니처 컴포넌트

Studio를 Studio답게 만드는 요소들. 새로 만들지 말고 이걸 재사용한다.
아래는 **현재 유지·재사용 가능한 컴포넌트**이며, 특정 구 페이지(신청 관리 / 상담 관리 / 미등록 학생관리)를
되살리라는 뜻이 아니다. IA는 §1.1을 따른다.

### 5.1 전환 파이프라인 바

통계 카드와 필터 칩의 중복을 대체하는 단일 요소. **숫자를 두 번 말하지 않는다.**

```
[신규 신청 5]→[일정 확정 3]→[체험 완료 8]→[등록 완료 2]  │ 취소 1 · 노쇼 0 · 미등록 4
                    등록 전환율 14.3%
```

- 각 스테이지는 **버튼이자 필터**. `aria-pressed`로 상태 표현, 활성 시 `--brand-50` 배경 + `--brand-500` 테두리.
- 스테이지 구성: 라벨(12px/600 `--text-2`) + 숫자(24px/800 tabular) + 보조문(11px `--text-3`, "1건 선생님 배정 필요"처럼 **행동을 지시**).
- 스테이지 사이 `›` 화살표 아이콘, `aria-hidden`.
- 우측에 종료 상태(취소·노쇼·미등록)를 구분해서 묶는다 — 퍼널이 아니라 참고값이므로 작게.
- 하단 푸터에 **등록 전환율**. 이 제품의 존재 이유이므로 항상 보이게 둔다.
- 대시보드 등 **요약이 목적인 화면에서만** 쓴다. Case 목록·상세에 KPI로 끼워 넣지 않는다.

### 5.2 캘린더 + 우측 패널 (예약시간 설정)

- 좌 1.6 : 우 1 그리드, 사이 `1px solid var(--border)` 세로 구분.
- **날짜 셀**: min-height 76px, radius `--r-md`. 내부 = 날짜(12/700) + 타임 수 pill(brand) 또는 휴무/마감 pill(gray) + 신청 건수 dot(우상단, `--text-1` 배경/흰 텍스트) + 잠금 🔒(우하단).
- 일요일 숫자 `--red-fg`. 지난 날짜 opacity 0.4 + 클릭 불가. 선택 시 `--brand-500` 테두리 + `--brand-50` ring 2px.
- **우측 패널**은 선택 날짜 하나만 다룬다. 날짜 미선택 시 빈 상태 문구.

### 5.3 슬롯 행 + 잠금 규칙

- radius `--r-md`, 시간이 13px/800으로 주인공. 정원은 `−/+` 스테퍼(24px, radius `--r-sm`).
- **신청이 있는 슬롯은 잠근다**: `--surface-sub` 배경, `--border-strong` 테두리, 배지 `신청 n건`(amber) + `🔒 수정 잠금`(gray), 삭제 버튼 자리에 "삭제 불가" 비활성 텍스트, 정원은 신청 건수 아래로 감소 불가.
- 닫힌 슬롯: opacity 0.45 + "열기" 복구 버튼(`--brand-700` 텍스트).
- 그 날짜에만 추가된 슬롯은 `이 날만` 배지(green).

### 5.4 규칙 편집기 (반복 설정)

슬롯을 한 줄씩 만들게 하지 않는다. **규칙 → 자동 생성**이 기본.

- 규칙 요약 행: 요일 도트(24px 사각, 선택 시 brand 채움) + `오후 4:00 ~ 오후 7:00 · 60분 · 정원 3 · 하루 3타임`.
- 편집 중인 규칙은 `--brand-500` 테두리 + `--brand-50` ring 3px + 배경 `--brand-50`.
- **실시간 생성 힌트 필수**: 입력이 바뀔 때마다 `→ 화·목요일마다 하루 3개 타임이 자동 생성돼요` (12px, `--brand-700`).

### 5.5 탭 위저드 (수업 등록·수정)

긴 폼은 한 화면에 쏟지 않는다.

- 상단 스텝 인디케이터 + 하단 **스티키 액션 바**(`position:fixed; left:var(--side-w); right:0`, `backdrop-filter: blur(8px)`).
- 스티키 바 구성: `임시저장`(Ghost) — 진행 안내(중앙, 12px `--text-3`) — `이전`(Ghost) / `다음`(Primary).
- 다음 버튼 비활성 시 opacity 0.35, `cursor:not-allowed`.
- 패널 전환은 `fadein .2s`. `prefers-reduced-motion` 존중.

---

## 6. 기본 컴포넌트

### 6.1 버튼

| 종류 | 스타일 | 용도 |
|---|---|---|
| Primary | `--brand-700` 배경 / 흰 텍스트 / hover `--brand-900` / radius `--r-md` / 패딩 `8px 16px` | **한 영역에 1개.** 저장, 확정 |
| Ghost | 흰 배경 / `--border` 1px / hover 시 테두리·텍스트 `--brand-700` | 추가, 취소, 보조 |
| Text | 배경 없음 / `--text-2` / hover 시 의미 색 | 삭제, 부가 |
| Danger | Ghost + `--red-fg` 텍스트, 또는 `--red-fg` 배경 | 파괴적 액션. confirm 필수 |

**검은색 버튼 금지.** 버튼 텍스트는 결과를 말한다: "저장"이 아니라 "규칙 저장", "확인"이 아니라 "일정 확정하기". 같은 액션은 흐름 내내 같은 동사.

### 6.2 카드

**Card는 아래 경우에만 쓴다.**

- 독립된 업무 단위
- 중요한 action
- 명확히 구분해야 하는 데이터 그룹

"Section이니까"라는 이유만으로 카드를 씌우지 않는다.
**Card inside Card 금지.** 정말 필요하면 코드 리뷰에서 이유를 설명해야 한다.

### 6.3 테이블 (목록형 화면)

- 6컬럼 이내로 유지. 날짜 중복 컬럼, 배정 상태 라벨 중복, slug 문자열 노출 금지 (**개발자용 문자열이 원장 화면에 나오면 안 된다**).
- 보호자명과 연락처는 한 셀에 합친다.
- **행 전체가 클릭 타깃.** 별도 액션 버튼 컬럼을 만들지 않는다.
- **처리 필요한 행은 좌측 3px `--brand-500` 레일.** 취소·노쇼 행은 opacity 0.55로 죽인다.
- 헤더 12px/700 `--text-2`, 셀 13px, 행 패딩 `12px 16px`, hover 시 `--surface-sub`.
- 900px 이하: 카드형으로 전환.

### 6.4 배지

`--r-full`, 11–12px/700, 패딩 `4px 8px`. 항상 `--*-bg` + `--*-fg` 쌍. 흰 배경 + 컬러 텍스트만 있는 배지 금지.

### 6.5 폼

- 인풋: 흰 배경, `--border` 1px, radius `--r-md`, 14px, 패딩 `8px 12px`. focus 시 `--brand-500` 테두리.
- 필드 라벨 12px/700 `--text-2`, 인풋 위 4px.
- 도움말은 인풋 아래 12px. 중립 정보는 `--text-3`, 결과 예고는 `--brand-700`.
- 에러는 `--red-fg` + 무엇이 잘못됐고 어떻게 고치는지. 사과하지 않는다.
- 토글 스위치 40×24px. on 색은 의미에 따라 — 활성화면 `--brand-700`, 차단(휴무 등)이면 `--red-fg`.

### 6.6 안내·경고 블록

- 정보 안내: `--brand-50` 배경 + `i` 아이콘 원형 + 제목(13px/700) + 본문(12px `--text-2`). 화면 상단 1개까지.
- 경고: `--amber-bg` 배경 + `--amber-fg` 텍스트, 12px, radius `--r-sm`. **조건부로만 노출** — 상시 노출되면 아무도 안 읽는다.

### 6.7 빈 상태 · 완료 상태

**빈 상태**: `--text-3`, 중앙 정렬, 2줄 이내, **다음 행동을 반드시 안내**한다.
`이 날짜에 생성된 타임이 없어요` / `아래에서 이 날짜에만 시간을 추가할 수 있어요` + 액션 버튼.
필터 결과 0건이면 `[조건 초기화]` 버튼 필수. 일러스트·이모지 남용 금지.

**완료 상태**: 인라인 피드백 · 토스트 · 도착한 화면 자체로 알리는 것이 기본이다.
전체 화면 성공 페이지는 온보딩 완료처럼 **정말 중요한 순간에만** 쓴다.
큰 일러스트를 기본 패턴으로 삼지 않는다.

### 6.8 오버레이 위계 — Modal / Drawer / Full Page

> **Modal = short task · Drawer = contextual edit · Full Page = complex workflow**

| | 쓰는 경우 | 예 |
|---|---|---|
| **Modal** | 짧은 결정, 확인, 한 가지 작업 | 일정 간단 추가, 삭제 확인, 짧은 상태 변경 |
| **Drawer** | 현재 맥락을 유지한 채 간단히 추가/수정 | 선생님 추가·수정, 간단한 metadata 편집, 후속 일정 설정 |
| **Full Page** | 여러 섹션·여러 판단·단계·Preview | 수업 등록 |

- **Modal 안에 긴 폼이나 위저드를 넣지 않는다.**
- **Drawer 는 Full Page 를 대체하지 않는다.** 지금 보고 있는 맥락과 무관한 내용을 Drawer 로 띄우지 않는다.
- 단순 작업을 Full Page 로 만들지 않는다.

### 6.9 보조 액션 — `···`

한 화면에 액션 버튼을 동시에 많이 노출하지 않는다.

- Primary 또는 그 화면의 핵심 액션 → **밖에 노출**
- 수정 · 복제 · 공개/비공개 · 삭제 · 기타 관리 → **`···` 컨텍스트 메뉴 우선 검토**

단 사용 빈도가 매우 높은 핵심 액션까지 억지로 메뉴 안에 숨기지 않는다.

---

## 7. UX 라이팅

- 존댓말 해요체. 원장을 가르치지 않는다.
- 시스템 용어 금지. 사용자가 하는 일로 부른다.
- 시간은 `오후 4:00`. **초 단위가 화면에 보이면 데이터 버그로 보고할 것** (`16:11:00` 같은 값).
- 요약은 판단이 끝나게: `8/14(목) 16:00 · 이번 주 3회 가능`. `외 39개 일정` `자세한 시간은 상세에서` 금지.
- 파괴적 confirm은 결과를 구체적으로: "이 운영 시간 규칙을 삭제할까요? 앞으로 생성될 예약 타임이 사라져요."
- 잠금 안내는 이유와 우회로를 같이: "이미 신청된 예약이 있는 타임은 유지돼요. 취소가 필요하면 상담·등록에서 개별 처리해 주세요."

---

## 8. 반응형

- 기본 데스크탑. `--studio-max: 1200px`.
- **900px 이하**: 사이드바 숨김(또는 상단 접힘), 2단 그리드 → 1단 스택, 테이블 → 카드형, 스티키 바 `left:0`, 액션 버튼 가로 배치.
- 원장은 데스크탑이 주지만 현장에서 폰으로 신청 확인을 한다. **상담·등록 목록과 상세는 모바일 동작 필수 검증 대상.**

---

## 9. 접근성 최소선

- 키보드 포커스 링 항상 보이게. `outline` 제거 금지.
- 필터 버튼 `aria-pressed`, 현재 메뉴 `aria-current="page"`, 장식 아이콘 `aria-hidden`.
- `@media (prefers-reduced-motion: reduce) { *{transition:none!important} }` 포함.
- 상태를 **색으로만** 전달하지 않는다. 배지에 텍스트를 같이 넣는다.
- semantic fg는 §2.1의 보정값을 쓴다. 작은 텍스트에 밝은 레거시 색을 되살리지 않는다.

---

## 10. 구독 / Entitlement 원칙

플랜은 **학원(organization) 단위**다. 계정마다 다른 권한을 갖지 않는다.
현재 판매하는 것은 **FREE / STANDARD** 두 가지이며, PRO는 도메인만 열어 두고 노출하지 않는다.

- FREE — 입점 · 모객 · 체험 운영(체험 완료까지)
- STANDARD — FREE 전체 + 체험 결과 · 상담 · 등록 전환 · 전환 분석

권한 판정은 `resolveStudioEntitlements` 가 만든 flag 하나만 본다.
**요금제 이름을 화면이나 action에서 직접 비교하지 않는다**(`if (plan === "standard")` 금지).

### 10.1 동일 Studio, 기능 깊이만 차등

- 플랜마다 서로 다른 IA나 서로 다른 Studio 레이아웃을 만들지 않는다.
- Core UX는 모든 플랜에서 동일하다.
- Feature entitlement에 따라 **기능 활성화 / advanced feature / automation / analytics의 깊이만** 차등한다.

### 10.2 잠금 UI

- 잠긴 기능은 **사용자의 업무 맥락 안에서** 안내한다.
- 사이드바에 잠긴 메뉴를 대량으로 추가하지 않는다.
- **UI-only gate 금지.** 숨기는 것은 보안이 아니다.
- **server entitlement 검증 필수.**
- **잠금은 화면을 통째로 막지 않는다.** 잠긴 것은 그 기능 하나이고, 나머지 업무는 그대로 돌아간다.
  Case Detail을 paywall로 덮거나 Dashboard 전체를 잠그지 않는다.
- **이미 저장된 데이터는 계속 보여준다.** 유료였다가 무료로 내려온 학원도
  과거 체험 결과와 상담 이력을 읽을 수 있다. 잠기는 것은 **새로 쓰기**뿐이다.
- **잠금 안내는 inline surface 하나로 끝낸다.** 잠긴 form/차트 자리에
  기존 card·빈 상태 문법(§6.2 · §6.7)을 그대로 쓰고, 새 색·새 카드 종류를 만들지 않는다.
  **modal·인터스티셜·토스트로 업그레이드를 띄우지 않는다.**
- 한 화면에 잠금 안내는 **최대 하나**다. 같은 맥락에서 반복하지 않는다.
- 문구는 무엇이 잠겼는지와 **기존 기록은 그대로 볼 수 있다는 사실**을 함께 말한다.
  가격·결제 문구는 여기서 다루지 않는다.

---

## 11. Reference 화면

새 화면을 만들거나 기존 화면을 개선할 때, 아래를 **기준 구현**으로 보고 맞춘다.

| 역할 | 화면 |
|---|---|
| **Reference List** | `/studio/cases` |
| **Reference Detail** | `/studio/applications/[id]` |
| **Reference Complex Form** | `/studio/classes/new` |

수업 관리 · 일정 관리 · 선생님 관리를 개선할 때는 위 세 화면과 이 문서의 원칙을 함께 따른다.
"레퍼런스와 다르게 만들 이유"가 설명되지 않으면 레퍼런스를 따른다.

---

## 12. 제품 범위 (Product Boundary)

**UI 레퍼런스는 기능 추가의 근거가 아니다.**
Base 에 있다는 이유만으로 첫수업에 기능을 만들지 않는다.

이 문서는 다음을 도입할 근거가 **아니다**: 메신저 · 범용 Task manager · 인보이스 ·
커머스 analytics · 개인 캘린더 · 알림 센터.

첫수업의 핵심은 하나다.

```
학부모/학생 신청 → 상담·확인 → 체험수업 → 체험 결과 → 후속 상담 → 등록 / 미등록
```

새 화면을 제안하기 전에 **이 흐름의 어느 단계를 돕는가**를 먼저 답한다.
답이 없으면 만들지 않는다.

---

## 13. Future Patterns — 아직 구현하지 않음

아래는 **방향**이지 구현 요구사항이 아니다. 지금 만들지 않는다.

- **Case Board View** — 단계 이동이 주 업무로 확인될 때 (§3.7)
- **Case Context Panel** — 목록에서 빠르게 확인이 필요할 때 (§3.3)
- **Split Form + Preview** — 학부모 전달용 리포트가 생길 때 (§3.5)
- **Calendar Day / Week / Month Workspace** — 일정 관리 개선 시 (§3.6)
- **Drawer 기반 선생님 편집기** — 선생님 관리 개선 시 (§6.8)

착수 전에 이 문서를 먼저 갱신하고, 그 다음 코드에 반영한다.

---

## 14. 금지 목록

1. 여기 없는 색·간격·크기를 새로 만들지 않는다. 필요하면 구현 말고 보고.
2. `#2AAD38`을 텍스트·버튼 배경에 사용 금지.
3. 검은색 버튼 금지.
4. 카드 box-shadow 금지 (모달·드롭다운·토스트·스티키 바 제외).
5. 4px 배수 아닌 간격 금지. 토큰에 없는 radius 금지.
6. 캔디 액센트 Studio 사용 금지.
7. 상태 라벨 하드코딩 금지 — 공용 상수 확장.
8. 인라인 스타일 금지 — module.css + 토큰.
9. 같은 숫자를 한 화면에서 두 번 표시 금지.
10. slug·id·enum 원문 등 개발자용 문자열 화면 노출 금지.
11. 신청이 존재하는 슬롯·일정의 삭제/축소 UI 노출 금지 — 잠금 처리.
12. 기존 컴포넌트·상수가 있으면 새로 만들지 말고 확장.
13. 같은 페이지의 주요 Section에 서로 다른 max-width 금지.
14. Card inside Card 금지.
15. Case Detail에 KPI 카드 금지.
16. DB에 없는 이력을 Timeline에 추측으로 생성 금지.

### v2 Anti-pattern

17. 외부 레퍼런스(Base) 화면을 그대로 복제 금지. 보라색 등 외부 palette 도입 금지.
18. 모든 것을 Card 로 감싸기 금지. 목록의 각 row 를 떠 있는 카드로 만들기 금지.
19. 장식용 차트 · 장식용 일러스트 금지.
20. 한 화면(한 영역)에 Primary CTA 2개 이상 금지.
21. 단순 작업을 Full Page 로, 복잡한 작업을 Modal 로 만들기 금지.
22. 현재 맥락과 무관한 Drawer 금지.
23. 필요 없는 Board / Timeline View 추가 금지.
24. KPI 를 위한 KPI(대시보드 카드 채우기) 금지.
25. 보조 액션 버튼 남발 금지 — `···` 우선(§6.9).
26. 디자인 때문에 업무 density 희생 금지(§0.4).
27. UI 레퍼런스를 근거로 제품 범위 확대 금지(§12).

---

## 15. 토큰 블록 (globals.css)

```css
:root{
  --brand-50:#EAF7EC;
  --brand-100:#D3EFD8;
  --brand-500:#2AAD38;
  --brand-700:#1B7A26;
  --brand-900:#12561A;

  --text-1:#1A1A1A;
  --text-2:#5C5C5C;
  --text-3:#8E8E8E;

  --bg:#F5F5F5;
  --surface:#FFFFFF;
  --surface-sub:#FAFAFA;

  --border:#E8E8E8;
  --border-strong:#D4D4D4;

  --green-fg:#1B7A26;
  --green-bg:#EAF7EC;

  --amber-fg:#8A5A00;
  --amber-bg:#FBF3E4;

  --blue-fg:#245EA8;
  --blue-bg:#EEF4FC;

  --gray-fg:#5C625C;
  --gray-bg:#F1F2F1;

  --red-fg:#B42318;
  --red-bg:#FDECEC;

  --r-sm:8px;
  --r-md:12px;
  --r-lg:16px;
  --r-full:999px;

  --s1:4px;
  --s2:8px;
  --s3:12px;
  --s4:16px;
  --s5:20px;
  --s6:24px;
  --s8:32px;
  --s10:40px;

  --studio-max:1200px;
  --side-w:260px;
}
```

---

## 16. 브랜드 — 변경하지 않는 것

- 첫수업 로고
- 현재 Studio 폰트
- 첫수업 Key Green 계열
- 브랜드 이름
- 전체적으로 밝고 차분한 SaaS 성격

v2 는 특정 외부 제품(Wise, Base 등)의 디자인을 복제하는 작업이 **아니다.**
참고하는 것은 원칙뿐이다: hierarchy · density · component consistency · action clarity ·
neutral surface usage · restrained decoration.

**Green 사용 범위**: Primary CTA · 활성 내비게이션 · 의미 있는 상호작용 · 선택/포커스 ·
핵심 브랜드 액센트. 화면 전체를 Green 으로 물들이지 않는다.

**Foundation 은 유지한다**: `--brand-*` · `--text-*` · `--bg` · `--surface` · `--surface-sub` ·
`--border` · `--border-strong` · semantic(`--green/amber/blue/gray/red-*`) · radius · spacing ·
`--studio-max` · `--side-w`, 그리고 현재 버튼·배지 Foundation. v2 에서 새 토큰을 만들지 않는다.

---

*v2 — 2026.09. 외부 SaaS 레퍼런스(Base)의 화면들을 검토해 **화면 문법**을 공식화했다.
Design Direction, Workspace/Density 원칙, Page Pattern 9종, 오버레이 위계(Modal/Drawer/Full Page),
보조 액션 `···`, Reference 화면, 제품 범위, Future Patterns, v2 Anti-pattern 을 추가했다.
토큰·버튼·배지·레이아웃 Foundation(UI-1A~1E)은 그대로 유지하며 새 토큰을 만들지 않았다.
v1.1 까지의 Case 규칙과 금지 규칙도 그대로 살아 있다.
변경 시 이 문서를 먼저 고치고 코드에 반영한다.*
